{
  // Solution-style root: the real settings live in the node + web projects. The
  // `typecheck` script checks both (with `--composite false --noEmit`); the IDE uses the
  // references to associate each file with the right project (DOM only in the renderer).
  "files": [],
  "references": [{ "path": "./tsconfig.node.json" }, { "path": "./tsconfig.web.json" }]
}

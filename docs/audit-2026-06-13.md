# Multi-Persona Code Audit — Private AI Drive Lite

_Date: 2026-06-13 · Scope: `apps/desktop/src` (124 files, ~24k LOC) + `docs/` + `model-manifests/` + `scripts/`_

This is a **working audit report**, not a design record — it lives outside the doc-lifecycle
rule (it documents open findings, not built design). Delete or fold it once the findings are
triaged/fixed.

> **Remediation status (2026-06-13):**
> - **Round 1 (commit `8cc2ea9`):** four code HIGH + OCR-window guards — H1 (lease leak),
>   H2 (RAG token budget), H3 (truncated-blob guard + regression test), H4 (rasterizer busy-flag),
>   M-S3 (OCR `setWindowOpenHandler`/`will-navigate`).
> - **Round 2:** H5 + M-A1 (one drift-detection vitest test — `script-drift.test.ts` — asserting the
>   `prepare-drive.{ps1,sh}` dir arrays, the `drive_format_version` literals, and the
>   `build-commercial-drive.ps1` runtime matrix against the canonical TS / `runtime-sources.yaml`),
>   and the three stale-doc findings M-D1 (test count), M-D2 (b9585 not b9196), M-D3 (catalog counts).
>
> - **Round 3 (branch `audit-2026-06-13-high-fixes`):** the prioritized MEDIUMs —
>   M-C1/M-C2/M-C3 (sidecar lifecycle: post-ready `'error'` fallback + stop() resolve, SIGKILL on
>   kill-throw, sync-throw re-arm), M-C4 (RRF tiebreak on best rank across both lists), M-C5 (abort
>   signal plumbed through retrieve→embed/rerank via `combineSignals`), M-S2 (per-handler IPC
>   arg-shape guards), and M-S1 (offline guard kept detection-only **by decision**, recorded in
>   `security-model.md`). Suite 1026 green; typecheck + build clean.
>
> - **Round 4 (branch `audit-2026-06-13-high-fixes`):** the a11y trio + the drift-test extension —
>   L8 (composer `aria-label`), M-U1 (always-mounted `role="alert"` `ErrorBanner`), L7 (sentence-throttled
>   `.sr-only` `StreamAnnouncer` for streaming, visible markdown no longer a live region), and M-A1
>   (script-drift test extended to the `config/{drive,policy}.json` payloads + the `verify-models`
>   sha256/runtime/format gates). Suite 1043 green; typecheck + build clean.
>
> - **Round 5 (branch `audit-2026-06-13-high-fixes`):** the remaining LOWs (except L16–L19). Correctness
>   batch — L2 (`cosineSimilarity` throws on length mismatch), L3 (E5 reorder: all-indexed/none-indexed,
>   throw on a mix), L4 (embedder `suspend()` clears the failed-start latch *after* teardown), L5
>   (transcriber `suspend()`/`stop()` await each child's transient-transcript shred), L6 (`parseCitations`
>   shape validation). a11y batch — L1 (link-scheme whitelist), L9 (`docs` literal → `{folder}` catalog
>   key), L10 (`friendlyIpcError` at the remaining sites), L11 (`<Spinner aria-hidden>`), L12
>   (`aria-describedby` on the ConfirmDialog body), L13 (debounced `.sr-only` strength announce), L14
>   (search-results `aria-live` + SR count), L15 (Thinking `<button aria-expanded>`). Suite 1058 green;
>   typecheck + build clean.
>
> Fixed items are marked **✅ FIXED** inline. Suite green after each round. The remaining items are the
> **deferred** architecture/deps LOWs **L16–L19** and the bigger **M-U2..6 / M-A2..5** UX & architecture
> refactors (lower value; not started — to be done only on request).

## How this was done

Five independent reviewers audited the codebase from distinct viewpoints, each verifying claims
against the actual source rather than the docs:

1. **Security auditor** — crypto/vault, offline guarantee, Electron hardening, IPC trust boundary, XSS.
2. **Correctness/reliability engineer** — races, error handling, RAG/runtime/DB edge cases.
3. **Docs-vs-code auditor** — contradictions between `docs/`/`BUILD_STATE.md` and the code.
4. **UX & accessibility auditor** — a11y, i18n in the UI, error/loading states, design-guideline fit.
5. **Staff engineer** — architecture, coupling, type safety, testing, maintainability, deps.

The headline HIGH findings (H1–H4 below) were re-verified by hand at the cited lines before
publishing.

## Overall assessment

**This is a strong, disciplined codebase.** Notable strengths the auditors independently confirmed:

- **Crypto/vault is genuinely solid:** AES-256-GCM with per-call random IVs (no reuse), Argon2id
  KDF with attacker-param DoS bounds, `timingSafeEqual` verifier, key zeroing on lock/unwrap/rewrap,
  a journaled atomic v1→v2 password-change migration with crash-recovery, startup plaintext-shred sweep.
- **Electron hardening is exemplary:** `contextIsolation`/`sandbox`/`nodeIntegration:false`, strict
  prod CSP (`script-src 'self'`, no inline/eval), deny-by-default `setWindowOpenHandler`/`will-navigate`,
  scoped audio-only permission handler. No XSS path via model output (react-markdown, no `rehype-raw`).
- **No hosted-AI calls, no telemetry, loopback-only sidecars** — the spec §0 hard rules hold.
- **Swappable-service architecture is textbook:** runtime/embedder/reranker/transcriber/OCR all sit
  behind narrow interfaces with DI seams; effectively zero `any`; uniformly guarded DB-column JSON parsing;
  zero TODO/FIXME/HACK debt; ~82 automated test files covering the spec-named invariants
  (never-throws, no-orphan runtime, GPU graceful degradation, vault data-safety).

No **CRITICAL** issues were found. The findings worth acting on are concentrated in a handful of
**permanent session-wide wedges** (H1, H4), **silent retrieval failure modes** (H2, H3), some
**accessibility/announcement** gaps, and **doc/script drift**.

---

## HIGH — fix these

### H1. ✅ FIXED — `importDocuments` leaks the vault document-work lease on a synchronous throw → password change wedged for the session
`apps/desktop/src/main/ipc/registerDocsIpc.ts:127-129`

`beginDocumentWork()` is called (line 127), then `expandPaths(paths)` (128) and
`createQueuedDocument(...)` per file (129) run **outside any try**. The `try/finally { releaseDocWork() }`
only begins inside the background IIFE (line 172-175). If `createQueuedDocument` throws (DB locked/closed
in a race, disk-full INSERT, constraint), the invoke throws and `releaseDocWork()` never runs.

**Impact:** `docWork` stays incremented forever; `changeWorkspacePassword` then permanently throws
`VaultBusyError` ("Documents are still being imported…") for the rest of the session, with no import
in progress to explain it. Restart-only recovery.

**Fix:** Wrap the synchronous setup (lines 128-139) in a `try` that calls `releaseDocWork()` on a
synchronous failure, or acquire the lease immediately before the IIFE.

### H2. ✅ FIXED — RAG context-token budget undercounts → grounded prompt can overflow the model window
`apps/desktop/src/main/services/rag/index.ts` (the `maxContextTokens` trim loop, ~L251-261)

The budget loop sums `approxTokenCount(c.text)` — a whitespace **word** count — and compares it
directly against `settings.ragMaxContextTokens` (model **tokens**) with **no** words→tokens factor.
Everywhere else in the codebase the conversion uses a 1.3 factor for exactly this
(`doctasks.ts` `SUMMARY_TOKENS_PER_WORD = 1.3`). Real BPE tokens run ~1.3–1.4× the word count, so the
assembled excerpt block can exceed the budget by ~30-40%.

**Impact:** The grounded prompt overflows; the runtime silently truncates, dropping excerpts (or the
trailing rules/question) → degraded or wrong document answers. Inconsistent with the conservative
budgeting the doc-task engine already applies.

**Fix:** Apply the same 1.3 factor against the limit, matching `SUMMARY_TOKENS_PER_WORD`.

### H3. ✅ FIXED — One corrupt/truncated embedding row throws and aborts the *entire* document query
`apps/desktop/src/main/services/embeddings/index.ts:144-145` (consuming `decodeVector`, L54-56)

The search loop guards a **`dimensions`-column** mismatch (`row.dimensions !== queryVector.length`,
line 144) and `continue`s — but it does **not** guard a `vector_blob` that is physically shorter than
`row.dimensions * 4` bytes (truncation, partial write). In that case `decodeVector` slices to
`dimensions*4` bytes, gets fewer, and `new Float32Array(buffer, 0, dimensions)` throws `RangeError`,
which propagates out of `search()` and aborts all retrieval.

**Impact:** A single bad row breaks *all* document search instead of skipping that chunk. Not in
`known-limitations.md`.

**Fix:** In the loop, skip + log when `row.vector_blob.length < row.dimensions * 4` (or wrap the
`decodeVector`/`cosineSimilarity` pair in a try/continue), mirroring the dimension-mismatch guard.

### H4. ✅ FIXED — Hidden-window PDF rasterizer permanently wedges if `new BrowserWindow` throws
`apps/desktop/src/main/services/ocr/rasterizer.ts:56-58`

`inUse = true` (line 56) is set **before** `new BrowserWindow(...)` (line 58); the `try/finally
{ inUse = false }` only starts after window creation and listener wiring. If `BrowserWindow`
construction throws (resource exhaustion, mid-quit, GPU-process crash), `inUse` stays `true` forever.

**Impact:** Every subsequent OCR run for the session fails with "PDF rasterizer is busy" until restart.

**Fix:** Move `inUse = true` inside the `try`, or reset it on a window-construction failure.

### H5 (docs). ✅ FIXED — `build-commercial-drive.ps1` hardcodes the runtime build matrix that already has a source of truth
`scripts/build-commercial-drive.ps1:222-229`

A literal 6-row `{family;dir;backend;bin}` table (win=vulkan, win/cpu, mac=metal, linux=vulkan,
linux/cpu, whisper win=cpu) duplicates `model-manifests/runtime-sources.yaml` (`builds[].extract_to`/
`backend`), which the canonical `commercial-drive.ts assertCommercialDrive` reads **dynamically** via
`planRuntimeDownload`. The script's own comment says "keep them in sync."

**Impact:** If a default backend or `extract_to` changes in the YAML, the TS gate updates automatically
but the PS1 cross-check keeps asserting the stale matrix — it can pass a drive the real gate would fail
(or vice-versa), and the `.sh` twin may already diverge. The worst config-drift surface, guarded by a
comment only.

**Fix (DONE):** Added `apps/desktop/tests/integration/script-drift.test.ts` — it regex-extracts the
`prepare-drive.{ps1,sh}` dir arrays, every `drive_format_version` literal, and the
`build-commercial-drive.ps1` runtime matrix, and asserts equality against `DRIVE_LAYOUT_DIRS`,
`DRIVE_FORMAT_VERSION`, and the `validateRuntimeSources`-parsed `runtime-sources.yaml`. Pure string
parsing, no shell execution — runs in CI. (Not yet covered: the `config/drive.json`/`policy.json`
payload literals and the sha256/supported-format gates — a follow-up could extend the same test.)

---

## MEDIUM

### Security / robustness

- **M-S1. ✅ RESOLVED (detection-only, by decision)** — Offline guard only LOGS, never blocks —
  `offlineGuard.ts:64-84`, wired `index.ts:348-358`. Decided with the user to KEEP detection-only and
  record the rationale in `security-model.md` ("Detection-only, not enforcement" under §2): enforcing
  via the process-wide `net.Socket.connect` shim would turn a host-extraction edge case into a hard
  offline failure breaking loopback IPC / the sidecar; the offline guarantee rests on the
  no-remote-code posture + the prod CSP, with the tripwire as defence-in-depth. Original text below.
  `installOfflineNetworkGuard` monkey-patches `net.Socket.prototype.connect`, calls `onViolation`
  (warn + audit), then **always calls the original connect**. It's a tripwire, not enforcement; the
  offline guarantee rests entirely on "no remote-calling code exists" + the CSP. This is a conscious
  trade-off (blocking could break loopback IPC/sidecar), but it is feasible to **reject non-loopback
  connects while offline** since `isLoopbackHost` already exempts the sidecar. Either upgrade to
  enforce-on-violation or record the detection-only decision explicitly in `security-model.md`.

- **M-S2. ✅ FIXED — IPC handlers trust renderer-supplied argument types** — `registerWorkspaceIpc.ts:79`,
  `registerDocsIpc.ts:122`, others. Args are TS-typed but not runtime-validated. `createWorkspace`
  evaluates `password.length` **outside** its try/catch (line 79), so a non-string password is an
  unhandled `TypeError` at the IPC boundary instead of a clean `{ ok:false }`. `importDocuments` passes
  `paths` to `expandPaths` without checking elements are strings (and accepts arbitrary machine paths,
  not just picker output). Several handlers do validate well (chat `mode` enum, dictation `instanceof
  Uint8Array` + size) — the pattern is known but applied inconsistently. Add small per-handler shape
  guards; the renderer is the untrusted boundary in Electron.

- **M-S3. ✅ FIXED — OCR hidden window lacks its own `setWindowOpenHandler`/`will-navigate`** —
  `rasterizer.ts:58-69` (fixed alongside H4). `webPreferences` are correct and channel isolation is good
  (`event.sender === win.webContents`), and the shared-session CSP applies, but this is the one place
  untrusted PDF bytes are rendered, and it has no window-level navigation/window-open guards like the
  main window. Add `setWindowOpenHandler(() => ({action:'deny'}))` + a `will-navigate` preventDefault.

- **M-C1. ✅ FIXED — Post-ready sidecar `'error'` (without `'exit'`) skips the GPU crash auto-fallback** —
  `runtime/sidecar.ts:256-273, 340-368`. `child.once('error', …)` only records `spawnError` (consulted
  in `waitForHealthy`); after start, an `'error'` without `'exit'` never fires `onUnexpectedExit`
  (gated on `this.exited`), so the documented crash auto-fallback (architecture.md GPU record §5.3)
  doesn't fire and `stop()` (which awaits only `'exit'`) can hang. Fire `onUnexpectedExit` from the
  `'error'` handler when ready && !stopping; resolve `stop()`'s wait on `'error'` too.

- **M-C2. ✅ FIXED — `stop()` returns without SIGKILL escalation when `child.kill()` throws → orphan sidecar** —
  `runtime/sidecar.ts:350-354`. On the kill-throw path `stop()` returns early without racing exit/grace
  or escalating to SIGKILL, leaving an orphan llama-server holding VRAM + the port. Still race exit and
  attempt SIGKILL on that path.

- **M-C3. ✅ FIXED — GPU crash auto-fallback self-disables if `restart` throws synchronously** —
  `runtime/factory.ts:284-298`. `restarting = true` then `void deps.restart(opts).catch().finally(...)`.
  A synchronous throw from `restart` escapes before the `.finally` is built, so `restarting` stays
  `true` forever and all future auto-fallbacks are suppressed. Wrap in `try { … } catch { restarting = false }`.

- **M-C4. ✅ FIXED — RRF tiebreak systematically suppresses keyword-only (exact-term) hits** —
  `rag/hybrid.ts:110, 118-120`. Keyword-only hits get `vectorRank = MAX_SAFE_INTEGER`; the tiebreak
  `rrfScore desc || vectorRank asc` means a #1 keyword-only chunk always loses the RRF-score tie to a
  #1 vector chunk — deprioritizing exact-term matches (invoice numbers, codes), the precise case hybrid
  search exists to catch. Tiebreak on the best individual rank across both lists.

- **M-C5. ✅ FIXED — Retrieval (query embed / rerank) ignores the caller's abort signal** —
  `embeddings/e5.ts:160`, `reranker/llama.ts:159`. Both use `AbortSignal.timeout(...)` only;
  `opts.signal` is threaded into `chatStream` but not into `retrieve`/`embed`/`rerank`. A user "Stop"
  during query embedding or the ~24.7 s CPU rerank (a known limitation) is ignored until completion.
  Plumb `opts.signal` through, combined via `AbortSignal.any([signal, AbortSignal.timeout(...)])`.

### Docs-vs-code

- **M-D1. ✅ FIXED — `BUILD_STATE.md` self-contradicts on the headline test count** — header/Phase-42 say
  **"1010 green"** (line 9-15, 810); §3 remediation says **"969"** (692-693); §5 "Current gate" says
  **"968/968"** (~1447). The header (1010) matches the i18n wave; §5 is stale (pre-i18n). Set one true
  number — re-run `npm test` and update §5/§3 to match.

- **M-D2. ✅ FIXED — `BUILD_STATE.md` §4 calls `runtime-sources.yaml` a b9196 PLACEHOLDER** (line 1258) but the
  file is pinned to the **real `b9585`** release with real URLs + SHA-256 (yaml line 43); §8 and
  `model-policy.md`/`architecture.md` already say b9585. A §4 reader would conclude no fetch is possible.
  Update line 1258.

- **M-D3. ✅ FIXED — Stale model-catalog counts** — `BUILD_STATE.md` §4 says "five manifests total" (922-923),
  §4/§8 say "six" (1257, 1528); `known-limitations.md:64` says "all six weights (~37 GB)";
  `model-manifests/README.md` lists only chat+embeddings subdirs. Actual: **11 manifests** (8 chat + E5
  + bge-reranker + whisper transcriber), and `model-manifests/` also has `reranker/`+`transcriber/`
  subdirs. The topic docs (`model-policy.md`, `drive-layout.md`) are correct and already supersede these
  — update the BUILD_STATE §4 contract lines + the manifests README (or point them at `model-policy.md`
  as the live list).

### UX / accessibility

- **M-U1. ✅ FIXED — Error Banner not reliably announced** — `components/Banner.tsx:37`. `role="alert"` was
  rendered only when `error` flipped truthy (alert element inserted already containing text); AT announces
  `role="alert"` reliably only when text changes *inside an already-present* region. Added `ErrorBanner`:
  an always-mounted `role="alert"` wrapper (`display:contents`) that swaps its message text and renders
  the visual Banner inside (Banner gained a `role` override so the inner one is `status`, not a nested
  alert). Migrated the chat/documents/models screen-level error banners to it.

- **M-U2. "Stop" during streaming gives no interruption feedback** — `chat/ChatScreen.tsx:338-339,
  293-307`. A stopped partial reply appears as a normal assistant turn; the user can't tell a truncated
  answer from a complete one. Mark stopped turns ("Stopped") or toast a brief confirmation.

- **M-U3. Model-not-running empty state bypasses the shared `EmptyState` component** —
  `ChatScreen.tsx:433-456` hand-rolls a `.card` with `<h1>/<h2>/<p>` + inline styles + a bare spinner
  instead of `EmptyState` (used everywhere else), breaking the design-guidelines §6 empty-state pattern.

- **M-U4. Two "Local · Offline" indicators can disagree** — sidebar `App.tsx:213` (refreshed only on
  screen/unlock change) vs a second self-fetching one in `ChatScreen.tsx:523`. Toggling network in
  Settings then navigating can show divergent states. Lift offline state to one source (§7 wants one
  ambient truth).

- **M-U5. ✅ FIXED — Number/size formatting bypasses locale in spots** — `ModelsScreen.tsx`
  (`{gb} GB` raw ×3 in the tech disclosure), `DiagnosticsTab.tsx` (drive read/write MB/s + tokens/s),
  `SettingsScreen.tsx` (context tokens). The tech-disclosure GB figures now go through a new
  `fmtGbNum(gb, lang)` (locale `toLocaleString`, no rounding of the manifest value); the throughput
  numbers through a new `fmtNum(n, lang)`; the context-token count through `toLocaleString(lang)`. EN
  output is byte-identical; German now gets the decimal comma / grouping like the surrounding `fmtGb`/
  `fmt1` cards. ModelsScreen test asserts the disclosure renders the GB figures via the formatter.

- **M-U6. No confirmation before expensive re-index** — `DocumentsScreen.tsx:367-381, 602`. "Re-index
  all stale" loops the whole corpus (multi-minute CPU) with no confirm and only a button-label spinner
  (no per-document progress). Delete already uses `ConfirmDialog` correctly; extend that + a `Progress`
  bar ("Re-indexing 3 of 12…") to re-index-all.

### Architecture

- **M-A1. ✅ FIXED — TS↔shell-script config drift guarded by comments only** — `DRIVE_LAYOUT_DIRS`
  (`drive.ts:35-53`) is re-spelled in `prepare-drive.{ps1,sh}`; `drive_format_version: 1` is hardcoded
  in 5 spots; the `config/drive.json`/`policy.json` payloads + the sha256 regex + runtime/format gates
  are triplicated across scripts. The no-Node-on-a-fresh-machine constraint makes the *duplication*
  unavoidable, so the fix is *detection*. `script-drift.test.ts` now covers: the dir arrays + the
  format-version literals (H5), **and** (this round) the `config/drive.json`/`policy.json` payload
  literals — parsed from both scripts and asserted against `buildDriveJson()`/`buildPolicyJson()` for
  both `--dev` and commercial — plus the `verify-models.{ps1,sh}` real-sha256 regex (vs `isRealSha256`)
  and the supported runtime/format gate (vs the now-exported `SUPPORTED_RUNTIMES`/`SUPPORTED_FORMATS`).

- **M-A2. Chat/RAG streaming-handler duplication** — `registerChatIpc.ts:107-186` vs
  `registerRagIpc.ts:34-119` repeat the guard preamble + the entire stream lifecycle (AbortController,
  `inFlight.set`, token-send-with-`isDestroyed`-guard, done/error/finally) verbatim. Two copies of the
  most safety-sensitive path must be kept in lockstep by hand. Extract a
  `withChatStream(ctx, event, conversationId, runFn)` wrapper.

- **M-A3. `initBackend()` (~245 lines) and `AppContext` (12 fields) trending toward god-objects** —
  `main/index.ts`, `services/context.ts`. `initBackend` also has three copy-paste model resolvers
  (`resolveEmbeddingModel`/`resolveRerankerModel`/`resolveTranscriberModel`, L54-112). Collapse the
  resolvers into `resolveModelByRole(...)` and extract a `composeServices(...)` builder; acceptable at
  current size but watch the growth.

- **M-A4. `doctasks.ts` is 1582 lines mixing three task pipelines** (summary/translation/compare) with
  parallel-but-separate window-math/prompt blocks inviting subtle divergence. Split into
  `doctasks/{summary,translation,compare,manager}.ts` behind the existing `DocTaskManager` facade.

- **M-A5. No automated test spawns a real binary / loads real weights / hits real HTTP** (by design —
  the zero-binary/zero-network CI posture). The riskiest integration surface (real `spawn`, llama-server
  SSE, `--list-devices` parsing, WASM OCR) is covered only by the 15 `PAID_*` manual harnesses. Keep the
  posture, but document the `PAID_*` matrix as a **required pre-release gate** and add canned-real-output
  regression fixtures (SSE, `--list-devices`) whenever an upstream format is observed to change.

---

## LOW

### Security / correctness
- **L1. ✅ FIXED.** Markdown link `href` scheme not whitelisted — `chat/Transcript.tsx`. A model could
  emit a `javascript:`/`data:` link; CSP + `setWindowOpenHandler` already block execution/navigation, so
  belt-and-suspenders. The `a` renderer now whitelists http(s) via `isSafeHttpUrl` — a disallowed scheme
  renders as inert `<span>` text, not a link.
- **L2. ✅ FIXED.** `cosineSimilarity` scored on `min(length)` instead of rejecting a mismatch —
  `embeddings/index.ts:61`. Now throws a `RangeError` on unequal lengths: the exported contract is
  "equal-length", the only in-tree caller dimension-guards first, so a mismatch reaching it is a real bug
  (a future caller mixing dimensions), not a prefix to score.
- **L3. ✅ FIXED.** E5 batch reorder collapsed a missing `index` to 0 — `embeddings/e5.ts:171-184`. The
  OpenAI embeddings schema makes `index` optional, so it now handles the two clean cases (all entries
  indexed → sort by index; none indexed → trust array order) and **throws** on a partial mix rather than
  silently misaligning vectors↔chunks (the count guard still passed).
- **L4. ✅ FIXED.** Embedder `suspend()` cleared the failed-start latch *before* teardown —
  `embeddings/e5.ts:205-221`. teardown awaits an in-flight start; a racing first-start that then failed
  re-armed `startFailed` after suspend had cleared it, so the next `embed()` threw stale (lock/unlock
  twice). Now clears `startFailed` *after* `teardown()`.
- **L5. ✅ FIXED.** Transcriber `suspend()/stop()` didn't await child exit — `transcriber/cli.ts:196-211`.
  On quit the parent could exit before whisper-cli's `'close'` fired the `finally` shred of the transient
  transcript JSON (default `workDir = tmpdir()`, never reached by the workspace crash-sweep) — un-shredded
  transcript **content** in the OS temp dir. Each child is now tracked against a promise that resolves only
  after its shred runs; `suspend()` kills then **awaits** those, so the parent cannot exit with an
  un-shredded transcript on disk.
- **L6. ✅ FIXED.** `rowToMessage` cast `citations_json` without shape validation — `chat.ts:133`
  (`as Citation[]`), while the sibling `parseScope` validated `Array.isArray` + per-element type. Added
  `parseCitations`/`isCitation`: require the `label`/`sourceTitle` strings, type-check the optional fields,
  drop bad elements — so a structurally wrong-but-valid-JSON payload no longer flows untyped to the renderer.

### UX / accessibility
- **L7. ✅ FIXED.** Streaming chat live region re-localized the whole markdown buffer every 40 ms flush —
  `chat/Transcript.tsx:116-123` — so `role="log"` either re-announced the full answer or went silent for
  screen readers during generation. The visible markdown is no longer a live region; a separate
  visually-hidden `StreamAnnouncer` (`role="log"`, `.sr-only`) announces only newly-completed sentences
  (boundary = `.!?…`/newline), markdown markup stripped, resetting on a new stream.
- **L8. ✅ FIXED.** Composer textarea had no accessible name — `chat/Composer.tsx:105-118` (placeholder
  only). Added `aria-label` mirroring the mode-specific prompt (the PasswordField pattern).
- **L9. ✅ FIXED.** Hardcoded English `docs` literal in the German UI — `HomeScreen.tsx:185` spliced
  `<strong>docs</strong>` between two translated fragments. Collapsed into a single catalog key
  (`home.preflight.continue`) with a `{folder}` placeholder; the UI splits on it to bold the literal
  folder name without hardcoding English word order. `continueBefore`/`continueAfter` removed.
- **L10. ✅ FIXED.** `String(e)` raw-error leaks bypassed `friendlyIpcError` — `ChatScreen.tsx`,
  `DocumentsScreen.tsx` (×2), `ModelsScreen.tsx` (×3). Now routed through `friendlyIpcError` so a
  transport-prefixed `Error: ECONN…` never renders to a non-technical user (the "codes only in
  Diagnostics" tone rule).
- **L11. ✅ FIXED.** Decorative `.spinner` spans not `aria-hidden`. Added a `<Spinner>` component with
  `aria-hidden` baked in (every call site pairs it with status text that carries the meaning); replaced
  every bare `<span className="spinner" />` (WorkspaceGate, Documents ×2, Chat, Settings, Models ×2,
  DictationButton).
- **L12. ✅ FIXED.** ConfirmDialog body not associated via `aria-describedby` — `components/Dialog.tsx`.
  The body now gets a `useId` id, wired into `aria-describedby` when a body exists (still `undefined`
  when there's none), so SR announces the "This permanently deletes…" sentence, not just the title.
- **L13. ✅ FIXED.** Password strength meter `role="status"` announced on every keystroke —
  `components/PasswordField.tsx`. Dropped the role from the visible meter (bar is `aria-hidden`, word is
  plain text); a separate `.sr-only` `role="status"` announces the strength word only after typing
  settles (debounced).
- **L14. ✅ FIXED.** Search results region not announced / no result count — `chat/ConversationList.tsx`.
  Added `aria-live="polite"` to the results region + an `.sr-only` count (`chat.search.count` plural key,
  "N results").
- **L15. ✅ FIXED.** Inline Thinking `<summary>` drove state via `preventDefault` and could desync the
  implicit `aria-expanded` — `chat/Transcript.tsx`. Replaced with a `<button aria-expanded>`
  (the `SourcesDisclosure` pattern); the reasoning text stays mounted and toggles `hidden` so the live
  buffer keeps accumulating while collapsed.

### Architecture / deps
- **L16. ✅ FIXED.** The `createSelected*` factory bodies were near-identical (~40 duplicated lines) —
  `{embeddings,reranker,transcriber}/factory.ts`. Extracted `resolveSidecarSelection(...)` in
  `services/select-sidecar-backed.ts`: the shared model→binary→weights ladder returning a discriminated
  `{available}` result, so each factory keeps only its real divergence (embedder degrades to
  `MockEmbedder`; reranker/transcriber return `null`) and its own `onSelect` labels. (OCR's selector is a
  different shape — a language-dir scan, no binary+weight pair — so it stays as is.) New
  `tests/unit/select-sidecar-backed.test.ts` covers the ladder; the existing factory tests stay green.
- **L17. ✅ FIXED.** `services/logging.ts` had zero automated tests — the only service with none, and it
  sits on every error path. Added `tests/unit/logging.test.ts`: past-`MAX_BYTES` rotation
  (`app.log`→`app.1.log` once oversized, fresh `app.log` after), no-rotation under the limit,
  circular-`meta` non-throw (`safeJson` falls back to `String(v)`), and `readLogTail` last-N + missing-file.
- **L18. ✅ FIXED.** `@napi-rs/canvas` (a native `.node` binary) is an optional transitive dep of
  `pdfjs-dist`, not imported in `src` — but it must **not** be bundled onto the portable drive
  (platform-specific, breaks the pure-JS/cross-OS posture). `includeSubNodeModules: true` would follow
  the hoisted tree and pull the win32 `.node` into app.asar. Added a `!**/node_modules/@napi-rs/canvas*/**`
  negation glob to `electron-builder.yml` `files`, and `tests/integration/packaging.test.ts` parses the
  YAML and asserts the exclusion exists **and** that its glob actually matches the real per-platform
  `@napi-rs/canvas-*` native-package paths.
- **L19. ✅ FIXED.** `runtime/gpu.ts` `--list-devices`/Vulkan parser was tested only against canned
  strings (real path = `PAID_GPU_SMOKE`); an upstream output-format change would mislabel backends and
  slip past CI. Captured the verbatim stdout from the pinned **b9585** Vulkan build (RTX 3080 Ti, dev box)
  into `tests/fixtures/list-devices-b9585-vulkan-rtx3080ti.txt` (CRLF preserved; `.gitattributes`
  `-text` keeps the bytes), and `gpu.test.ts` now parses that fixture as a regression assertion
  (asserts the CRLF is present and `parseListDevices` lands the device). Coordinated with M-A5`s
  canned-real-output policy.

---

## Confirmed NON-issues (verified, recorded so they aren't re-flagged)

- Crypto: random IVs per call (no reuse), CSPRNG throughout, `timingSafeEqual` verifier, KDF param DoS
  bounds, key zeroing — all correct.
- No hosted-API calls anywhere; sidecar binds 127.0.0.1 only; CSP dev/prod split correct; deny-by-default
  permission handler.
- No path traversal in document storage (stored paths = server `randomUUID()`; lookups are DB-keyed).
- No XSS via model output (react-markdown, no `rehype-raw`/`dangerouslySetInnerHTML`).
- `deleteConversation`/`deleteDocument` FK delete order correct (children before parent, FKs on).
- DB migrations idempotent (`ensureColumn` pragma-guarded + identifier-validated; FTS existence-guarded).
- `inFlightStreams` canceller never clobbers another conversation; doc-task vs chat one-at-a-time guard
  is symmetric.
- Effectively zero `any`; all DB-column JSON parses guarded except L6; `updateSettings` validates
  type + enum on write.
- Zero TODO/FIXME/HACK and zero `@ts-ignore`/`eslint-disable` in `src`.
- All code `§`-anchor citations (i18n record, GPU record, wave-3 plan, rag-design §10/11/12) resolve to
  existing, stably-numbered sections; no comment points to a deleted plan file.
- `DEFAULT_SETTINGS`, `DEFAULT_KDF`/`SCRYPT_KDF`, the IPC channel list, and `AuditEventType` (25 members)
  all match the docs.
- Accepted/documented limitations confirmed accurate: plaintext working DB while unlocked (no SQLCipher
  in `node:sqlite`), best-effort SSD shred, `findFreePort` TOCTOU, per-import jobs map not pruned,
  `getSettings` no read-side type-guard, checksum-cache same-size/mtime tamper, Intel-Mac wrong-arch
  ladder falling to mock.

---

## Suggested triage order

1. **H1, H4** — the two permanent session-wide wedges (one-line scope fixes each).
2. **H3, H2** — silent retrieval failures (one bad row kills all search; context overflow).
3. ✅ **M-C1/M-C2/M-C3** — sidecar lifecycle leaks + the self-disabling GPU auto-fallback (DONE, round 3).
4. **H5 + M-A1** — the one drift-detection vitest test (kills two config-drift findings at once).
5. **M-D1/M-D2/M-D3** — the BUILD_STATE §4 stale contract lines (test count, b9585, catalog counts).
6. **M-U1/M-U2 + L7/L8** — the highest-impact a11y/UX (error & streaming announcement, unlabeled input).
7. The remaining MEDIUMs (IPC arg guards, RRF tiebreak, abort plumbing, refactors) and LOWs as capacity allows.

id: qwen3.5-4b-ud-q4kxl
display_name: Qwen3.5 4B (UD-Q4_K_XL)
family: qwen3.5
role: chat
format: gguf
runtime: llama_cpp
license: apache-2.0
size_on_disk_gb: 2.9
recommended_min_ram_gb: 8
recommended_ram_gb: 16
# Rank 3 (owner decision 2026-07-12, newest-generation preference; model-benchmarks.md §6.4):
# a subjective owner judgment that newer model generations improve on the ones they replace, so
# the newest-generation model is recommended per tier EVEN THOUGH the #48 tester eval favors the incumbent here
# (qwen3-4b F1 .3277 vs .2728; but F1 is length-confounded per the §9 scorer caveats, EM ties at
# .9647 — a cross-run i9-vs-i7 comparison — so the eval is treated as directional, not decisive). The two
# old gates are otherwise satisfied: it HAS been through the tester eval (b9849 binary), and
# the b9849 load gate was observed directly on 2026-07-12 (loaded + streamed through the app
# from the portable drive, rung 1, GPU backend). The #53 field signal (best speed/quality
# trade-off on a weak 16 GB laptop) now counts as supporting context, not the promotion basis.
# Picker effect (verified against recommendModelIdByRam, with the full 2026-07-12 promotion
# set): wins the ≤12 GB runnable tiers from qwen3-4b-instruct-q4 (rank 2, same min 8); does NOT
# steal 16-20 GB because the promoted 9B (same rank 3, same recommended_ram_gb 16, bigger on
# disk) wins that tier on the disk-size tiebreak.
recommendation_rank: 3
recommended_context_tokens: 4096
# Qwen3.5 operates in thinking mode by default (emits <think>...</think>), so the chat
# template honours `enable_thinking` → the Deep answer mode applies (like the original 4B).
supports_thinking_mode: true
supports_tools: true
local_path: models/chat/qwen3.5-4b-ud-q4kxl.gguf
# Real hash captured 2026-06-18 by fetching the upstream weight and computing SHA-256
# (file bytes matched download.size_bytes exactly: 2,912,109,728).
sha256: b252c5610a42ca82d20fe2a12813e9d069eed89292907e26c783eeb0bc961bc7
# Quantization choice (user decision 2026-06-18): unsloth's "Dynamic 2.0" UD-Q4_K_XL — keeps the
# precision-sensitive layers at higher bit depth and squeezes the rest, so it beats plain Q4_K_M
# on quality at almost the same footprint (2.91 vs 2.74 GB; both stay in the 8 GB-RAM tier). For a
# small 4B model the bit depth bites harder than at 8–14B, so the richer Q4 is worth +0.17 GB.
# The Qwen org publishes no official GGUF for the 3.5 refresh → established-quantizer fallback
# (the same posture as the qwen3-4b-instruct-2507-q4 entry): unsloth, the most-used source.
download:
  url: https://huggingface.co/unsloth/Qwen3.5-4B-GGUF/resolve/main/Qwen3.5-4B-UD-Q4_K_XL.gguf?download=true
  sha256: b252c5610a42ca82d20fe2a12813e9d069eed89292907e26c783eeb0bc961bc7
  size_bytes: 2912109728
  license_url: https://huggingface.co/Qwen/Qwen3.5-4B/blob/main/LICENSE
bundled_on_preconfigured_drive: false
# b9849 RUNTIME SMOKE: SATISFIED 2026-07-12. The runtime was bumped to llama.cpp b9849
# (2026-06-30, runtime-sources.yaml) as the Qwen3.5 compatibility gate; this model was then
# observed loading + streaming through the app from the portable drive on b9849 (rung 1, GPU
# backend, 2026-07-12), and the #48 tester eval ran it on the same b9849 binary. NOTE: upstream
# is a VISION model; this `.gguf` runs TEXT-ONLY (the mmproj projector is not shipped), which
# is all our chat pipeline uses.
recommended_profiles: []
license_review:
  status: approved
  reviewed_by: "project maintainer (Claude-assisted review, HF card verification)"
  reviewed_at: "2026-06-18"
  notes: "APPROVED 2026-06-18: base model Qwen/Qwen3.5-4B is apache-2.0 (per the unsloth GGUF model card; LICENSE blob expected at download.license_url). QUANTIZATION PROVENANCE: the Qwen org publishes no official GGUF for the 3.5 refresh, so this manifest pins the established quantizer unsloth (https://huggingface.co/unsloth/Qwen3.5-4B-GGUF, card tag apache-2.0) — a third-party requant of apache-2.0 weights, NOT a vendor GGUF; same posture as the qwen3-4b-instruct-2507-q4 entry. Apache-2.0 permits commercial redistribution; ship the LICENSE/NOTICE attribution with the drive. NOT yet bundled (bundled_on_preconfigured_drive: false) and not benchmarked; the runtime was bumped to b9849 (2026-06-30) as the Qwen3.5 gate but b9849 still REQUIRES a manual load smoke before this model is promoted (see comment above). UPDATE 2026-07-12: promoted to recommendation_rank 3 (owner decision, newest-generation preference; model-benchmarks.md §6.4) and now the <=12 GB auto-pick; the b9849 load smoke is SATISFIED (loaded + streamed through the app from the portable drive, rung 1, GPU backend, 2026-07-12 — see the comment above). Supersedes the 'not benchmarked / b9849 still REQUIRES a manual load smoke before promotion' precondition above."

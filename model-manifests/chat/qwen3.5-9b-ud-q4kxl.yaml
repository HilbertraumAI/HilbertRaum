id: qwen3.5-9b-ud-q4kxl
display_name: Qwen3.5 9B (UD-Q4_K_XL)
family: qwen3.5
role: chat
format: gguf
runtime: llama_cpp
license: apache-2.0
size_on_disk_gb: 6.0
# 12 GB minimum is intentionally conservative: the file is ~5.97 decimal GB but Electron +
# SQLite + the embedding/reranker sidecars + KV cache all need headroom alongside it.
recommended_min_ram_gb: 12
recommended_ram_gb: 16
# Rank 3 (owner decision 2026-07-12, newest-generation preference; model-benchmarks.md §6.4):
# a subjective owner judgment that newer model generations improve on the ones they replace, so
# the newest-generation model is recommended per tier. The #48 tester eval ranked this UNDER the
# incumbent Ministral 3 8B ONLY on the hallucination-trap axis (the `en-contract-penalty`
# invoice-distractor trap, where Ministral is clean); on F1/EM the 9B actually EDGED Ministral
# within cross-run tolerance (F1 .3152/.3124 vs Ministral's Phase-29 i7 .3111, EM .9765 vs .9529)
# — and F1 is length-confounded per the §9 scorer caveats anyway, so the eval is treated as
# directional, not decisive. It
# HAS been through the tester eval on the b9849 binary (both this UD-Q4_K_XL and the local Q8
# ran), which also discharges the old load-smoke gate. Picker effect (with the full 2026-07-12
# promotion set): wins 16-20 GB from Ministral (rank 2); within the promoted pair at this tier
# (the 4B carries the same rank 3 and recommended_ram_gb 16) it wins on the disk-size tiebreak.
recommendation_rank: 3
# Native context is 262,144 tokens (extensible to ~1,010,000 via YaRN). HilbertRaum recommends
# 8192 here because this field is the SAFE LOCAL RUNTIME context budget for normal laptops, not
# the theoretical model maximum. Revisit only after KV-cache/RAM budgeting + a long-context eval.
recommended_context_tokens: 8192
# Qwen3.5 0.8B/2B/4B/9B have reasoning DISABLED by default in unsloth's llama.cpp examples
# unless `enable_thinking=true`; the chat template still honours the switch, so the Deep answer
# mode applies. Verify the live runtime behaviour by smoke test (BUILD_STATE Qwen3.5 wave).
supports_thinking_mode: true
supports_tools: true
local_path: models/chat/qwen3.5-9b-ud-q4kxl.gguf
# Real hash CONFIRMED 2026-07-03 against HuggingFace LFS metadata (git-LFS OID = the file's SHA-256,
# cross-checked with the resolve `X-Linked-ETag`/`X-Linked-Size`). Unlike the 27B/35B, the 9B's wave
# value + size_bytes were already correct, so its download verified fine — kept as-is (dl-size-cap-2026-07-03).
sha256: 6f5d30666c2d8ae16a306e616d95341dcf3cc46810df84d7e6f5a7d1e4c1b293
# Quantization: unsloth "Dynamic 2.0" UD-Q4_K_XL (keeps the precision-sensitive layers at higher
# bit depth, squeezes the rest) — preferred over plain Q4_K_M unless a smoke proves it cannot
# load. The Qwen org publishes no official GGUF for the 3.5 refresh → established-quantizer
# fallback (the same posture as the qwen3.5-4b-ud-q4kxl + qwen3-4b-instruct-2507-q4 entries).
download:
  url: https://huggingface.co/unsloth/Qwen3.5-9B-GGUF/resolve/main/Qwen3.5-9B-UD-Q4_K_XL.gguf?download=true
  sha256: 6f5d30666c2d8ae16a306e616d95341dcf3cc46810df84d7e6f5a7d1e4c1b293
  size_bytes: 5966095584
  license_url: https://huggingface.co/Qwen/Qwen3.5-9B/blob/main/LICENSE
bundled_on_preconfigured_drive: false
# TEXT-ONLY USE: upstream Qwen3.5 is a vision-language model, but this chat manifest ships ONLY
# the language GGUF and no mmproj/projector — which is all our chat pipeline uses.
recommended_profiles: []
license_review:
  status: approved
  reviewed_by: "project maintainer (Claude-assisted review, HF card/LFS verification)"
  reviewed_at: "2026-07-01"
  notes: "APPROVED 2026-07-01: base model Qwen/Qwen3.5-9B and Unsloth GGUF card are apache-2.0. Quantization provenance: Unsloth Dynamic 2.0 GGUF, third-party quantization of Apache-2.0 Qwen weights. Apache-2.0 permits commercial redistribution; ship LICENSE/NOTICE attribution with the drive. TEXT-ONLY USE: upstream is a vision-language model, but this chat manifest ships only the language GGUF and no mmproj/projector. Not bundled and not auto-recommended until the HilbertRaum offline benchmark + b9849 runtime smoke pass. UPDATE 2026-07-12: promoted to recommendation_rank 3 (owner decision, newest-generation preference; model-benchmarks.md §6.4) and now the 16-20 GB auto-pick; the #48 tester eval ran it on the b9849 binary, discharging the load-smoke gate (the §9.1 through-the-app smoke remains open). Supersedes the 'not auto-recommended until...' precondition above."

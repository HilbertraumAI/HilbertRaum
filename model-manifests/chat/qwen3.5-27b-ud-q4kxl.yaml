id: qwen3.5-27b-ud-q4kxl
display_name: Qwen3.5 27B (UD-Q4_K_XL)
family: qwen3.5
role: chat
format: gguf
runtime: llama_cpp
license: apache-2.0
size_on_disk_gb: 17.6
# RAM is intentionally conservative: the file is ~17.6 decimal GB and the KV cache + Electron +
# database + embedding/reranker sidecars all need headroom alongside it.
recommended_min_ram_gb: 24
recommended_ram_gb: 32
# Default 0: NOT auto-recommended. High-end DENSE candidate intended to challenge Gemma 4 12B,
# Qwen3 14B, and Qwen3 30B-A3B on 32 GB machines. Qwen3.5 27B public benchmarks look stronger
# than 35B-A3B on several language/instruction/coding categories, BUT HilbertRaum promotion
# depends ONLY on the local German/English grounded-QA benchmark + manual smoke — not the public
# scores. Promote with a real `recommendation_rank` only after it wins locally on b9849.
recommendation_rank: 0
# Native context is 262,144 tokens (extensible to ~1,010,000 via YaRN). Manifest runtime context
# is 8192 for now — the safe local runtime budget for normal laptops, not the model maximum.
# Revisit only after KV-cache/RAM budgeting + a long-context eval.
recommended_context_tokens: 8192
# Larger Qwen3.5 models operate in thinking mode by default; the chat template honours
# `enable_thinking`, so the Deep answer mode applies. Verify live behaviour by smoke test.
supports_thinking_mode: true
supports_tools: true
local_path: models/chat/qwen3.5-27b-ud-q4kxl.gguf
# Real SHA-256 + exact byte size captured 2026-07-03 from HuggingFace LFS metadata — the git-LFS OID IS
# the SHA-256 of the file content, cross-checked against the resolve `X-Linked-ETag`/`X-Linked-Size` (the
# object the app downloads) and proven equal to a full download+sha256sum via qwen3.5-4b. The 2026-07-01
# wave's original value (4e3d432f…) did NOT match the real file and, with the then-understated size_bytes,
# tripped the download size cap at ~95% — the reported bug (dl-size-cap-2026-07-03).
sha256: 13cb6228344898afa50d963c02ae0d991ae25094eea8837db8d0e452e91c5888
# Quantization: unsloth "Dynamic 2.0" UD-Q4_K_XL, NOT plain Q4_K_M, unless a smoke shows the
# dynamic quant cannot load (then add qwen3.5-27b-q4km.yaml as the experimental fallback). The
# Qwen org publishes no official GGUF for the 3.5 refresh → established-quantizer fallback.
download:
  url: https://huggingface.co/unsloth/Qwen3.5-27B-GGUF/resolve/main/Qwen3.5-27B-UD-Q4_K_XL.gguf?download=true
  sha256: 13cb6228344898afa50d963c02ae0d991ae25094eea8837db8d0e452e91c5888
  # Exact byte size from HF LFS (X-Linked-Size); the real file is ~5.3% larger than the wave's original
  # 16,729,015,680 estimate — that understatement is what tripped the old exact-size download cap at ~95%.
  size_bytes: 17621125024
  license_url: https://huggingface.co/Qwen/Qwen3.5-27B/blob/main/LICENSE
bundled_on_preconfigured_drive: false
# TEXT-ONLY USE: upstream Qwen3.5 is a vision-language model, but this chat manifest ships ONLY
# the language GGUF and no mmproj/projector — which is all our chat pipeline uses.
recommended_profiles: []
license_review:
  status: approved
  reviewed_by: "project maintainer (Claude-assisted review, HF card/LFS verification)"
  reviewed_at: "2026-07-01"
  notes: "APPROVED 2026-07-01: base model Qwen/Qwen3.5-27B and Unsloth GGUF card are apache-2.0. Quantization provenance: Unsloth Dynamic 2.0 GGUF, third-party quantization of Apache-2.0 Qwen weights. Apache-2.0 permits commercial redistribution; ship LICENSE/NOTICE attribution with the drive. TEXT-ONLY USE: upstream is a vision-language model, but this chat manifest ships only the language GGUF and no mmproj/projector. Not bundled and not auto-recommended until the HilbertRaum offline benchmark + b9849 runtime smoke pass."

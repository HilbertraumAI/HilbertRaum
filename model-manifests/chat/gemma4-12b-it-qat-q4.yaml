id: gemma4-12b-it-qat-q4
display_name: Gemma 4 12B Instruct QAT Q4
family: gemma4
role: chat
format: gguf
runtime: llama_cpp
license: apache-2.0
size_on_disk_gb: 7.0
recommended_min_ram_gb: 14
# recommended_ram_gb recalibrated 32→24 (issue #48, model-benchmarks.md §6.3): measured ~10.6 GiB
# peak RSS + embedder/reranker/app/OS co-residency fits a 24 GB machine comfortably — 20–24 GB
# machines were stuck on the 8B tier because nothing was tuned between 16 and 32.
recommended_ram_gb: 24
recommendation_rank: 2  # Phase-29 12-14B WINNER (beats Qwen3 14B on every axis) — preferred at tier
recommended_context_tokens: 8192
# Context cache at 8k under the app launch (b9849 defaults: 4 unified slots, ubatch 2048): 512 MiB
# full-attention + 1,920 MiB sliding-window cells (40 layers x 6,144 cells) = 2.375 GiB; the
# picker's graphics-fit cache term (model-benchmarks.md §6.6, PR #308 audit decision 11).
estimated_context_cache_gib: 2.4
supports_thinking_mode: true  # Phase-29 run #2: thinking-quality check passed (Deep 8/8 = Balanced 8/8, real reasoning) → flipped
supports_tools: false
local_path: models/chat/gemma4-12b-it-qat-q4.gguf
sha256: 93567e57a8fe10b23569b9d9ec38cd005deedf71e29477c421a4b83f418a538b
# Phase 28 (model-catalog-expansion-plan D16): where `fetch-models` downloads the weight
# from + what to verify it against. Filename + byte size verified against the HF repo file
# listing 2026-06-10 (the file name is lower-case `gemma-4-12b-...` while the repo id keeps
# `12B`); hash promoted via `verify-models --generate` after the first fetch.
# REPOINTED IN PLACE 2026-08-20 (issue 201) — both sha256 fields + size_bytes. Google re-uploaded
# this checkpoint on 2026-07-17 (repo commit 29d09777, "corrected vocabulary"), so the URL stayed
# alive while serving different bytes: the old pin faff1a63... failed SHA-256 verification at the
# END of a ~7 GB download. Re-measured on the i9-9900X + RTX 3090 rig 2026-08-20, old checkpoint vs
# corrected, same harness, same day, temp 0. WHAT WAS MEASURED: §2 grounded-QA (100 items),
# peak RSS + peak VRAM at ctx 8192 on the b10430 basis, and the full §9.1 in-app leg set on the
# b9849 pin. VERDICT: the same model on every surface we ship — all 100 §2 answers BYTE-IDENTICAL
# to the old checkpoint (mean F1 .3399, EM .9765, citation .9882, hallucination .0667 — unchanged,
# so the rank-2 standing carries on evidence), peak RSS delta 44 kB (7,917,560 → 7,917,516 kB, i.e.
# zero), peak VRAM identical at 9,720 MiB, all §9.1 legs green. In place rather than a successor id
# (the issue-196 posture) because the file was SUPERSEDED, not deleted, and two provably
# indistinguishable catalog entries would be the successor cost -- see model-benchmarks.md §9.6.
# LOADER OBSERVATION, saved here because it looks alarming and is not: the corrected file emits TWO
# control-token override warnings the old file does not ("control-looking token '</s>' was not
# control-type ... will be overridden", same for '<|tool_response>'). llama.cpp re-types those
# tokens; neither plays any part in our chat-template path, so the warnings are inert.
# Raw rows: eval/results/i9-9900X-gemma4fix-{old,new}-vulkan-{items.jsonl,quality.csv}.
download:
  url: https://huggingface.co/google/gemma-4-12B-it-qat-q4_0-gguf/resolve/main/gemma-4-12b-it-qat-q4_0.gguf?download=true
  sha256: 93567e57a8fe10b23569b9d9ec38cd005deedf71e29477c421a4b83f418a538b
  size_bytes: 6975879296
  license_url: https://www.apache.org/licenses/LICENSE-2.0
bundled_on_preconfigured_drive: false
# PROMOTED challenger (Phase 29, §5.4): beats the incumbent Qwen3 14B on EVERY axis in the
# first benchmark run — fewer hallucinations (1 vs 2/15), faster decode (3.0 vs 2.1 tg t/s,
# i7-1185G7 CPU), same EM/F1, same RAM. (Both are Apache-2.0 — the whole catalog is; the edge
# is quality + speed, not licence.)
# recommended_min_ram_gb dropped 16→14 from the measured ~10.6 GiB peak RSS;
# recommended_profiles stays [] (prod picker is RAM-best-fit); its 12-14B-tier win is applied via
# `recommendation_rank: 2` — the quality-aware picker (docs/model-benchmarks.md §6.2) prefers it
# over Qwen3 14B on 24 GB+ machines (issue #48). High tier
# (Qwen3 14B class), 140+ languages. Vendor QAT **Q4_0** quantization, not Q4_K_M —
# quantization-aware trained, so higher quality than a naive Q4 at similar size. Needs
# llama.cpp ~b8607+; our pinned b9585 (2026-06-09) covers it (plan §3.1). TEXT-ONLY USE: the
# upstream repo also ships an mmproj vision file — we deliberately do not reference it.
# THINKING: Gemma 4's chat template honours `enable_thinking` (Deep streams real
# `reasoning_content`). `supports_thinking_mode` was FLIPPED to true (Phase-29 run #2): the
# thinking-quality check (tests/manual/gemma-thinking.test.ts) had Deep match Balanced 8/8 on
# reasoning items (incl. the snail/bat-ball/syllogism traps) with coherent chain-of-thought, so
# Deep is safe to offer. Caveat: both modes scored 100%, so the small set can't show Deep
# *strictly* helps — it shows Deep deliberates well and never regresses. The only thinking-capable
# challenger; the composer now offers "Thorough" for Gemma 4.
recommended_profiles: []
license_review:
  status: approved
  reviewed_by: "project maintainer (Claude-assisted review, HF API verification)"
  reviewed_at: "2026-06-10"
  notes: "APPROVED 2026-06-10: license verified as apache-2.0 via the Hugging Face API card tag on the OFFICIAL Google org GGUF repo (https://huggingface.co/google/gemma-4-12B-it-qat-q4_0-gguf) — Gemma 4 is the first Gemma generation under Apache-2.0 (announcements: https://blog.google/innovation-and-ai/technology/developers-tools/gemma-4/ and https://opensource.googleblog.com/2026/03/gemma-4-expanding-the-gemmaverse-with-apache-20.html; Gemma 3 and earlier keep the custom Gemma Terms and stay disqualified). Quantization published by the model vendor itself (official Google QAT GGUF — not a third-party requant). Apache-2.0 permits commercial redistribution; ship the LICENSE/NOTICE attribution with the drive (download.license_url points at the canonical Apache-2.0 text; the repo publishes no LICENSE blob, the card tag is the declaration)."
